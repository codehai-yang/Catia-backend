# 2026-09-23

## getglb 接口改造：把「零件实例名」写进 glTF 节点名

需求：前端要按零件实例名在数模上高亮 CATIA 选中项，后端必须给出唯一标识。

**改之前的坑（关键发现）**：`app/utils/step_to_gltf.py` 旧实现把所有叶子零件合并成一个
`TopoDS_Compound`、再整体 `AddShape` 一次，所以生成的 GLB 里只有一个节点、且没有名字，
前端根本无法定位到单个零件去做高亮。这不是加个字段的事，必须改导出结构。

### 改动内容

- `app/utils/step_to_gltf.py`（重写）
  - 每个零件 `shape_tool.AddShape(shape, False)` 成为一个独立 XDE 标签（= 一个 glTF 节点）。
  - `TDataStd_Name.Set(label, 实例名)` 写节点名；writer 设
    `SetNodeNameFormat(RWMesh_NameFormat_InstanceOrProduct)`。
  - `step_to_gltf()` 新增 `name` 参数；`steps_to_gltf()` 的 entries 兼容 2 元组 / 3 元组
    `(step_path, matrix, name)`，旧的二元组调用不破。
  - 顺带修复：原来的 `sys.exit(1)` 改为 `raise RuntimeError`（在 FastAPI 线程池里抛 SystemExit 很危险）。
- `app/services/catia/service.py`
  - 新增 `CatiaService._instance_name(product, index)`：取 `Product.name`，取不到时兜底 `part_<i>`。
  - `_export_instances_to_glb` / `_export_reference_to_glb` 把实例名透传给导出层。
- 接口签名与响应格式**不变**（仍返回二进制 GLB）。唯一标识 = glTF `node.name` = CATIA 实例名，
  与 `/getselected` 返回的 `parts[].name` 同源（都是 `Product.Name`），前端可直接对齐。

### 验证

`_verify_named_glb.py` 端到端跑通：3 个 STEP → GLB 里 3 个命名节点 + 3 个 mesh，
scene roots = [0,1,2]，旧 2 元组调用退化为 `part_0`/`part_1`。
`app/utils/step_to_gltf.py` 与 `app/services/catia/service.py` 均编译 + 导入通过。

### 环境

运行/验证用 `F:\office\conda\envs\catia310\python.exe`（OCC、pycatia、win32com 都在这）；
项目根目录的 `.venv` 里没有 OCC。

---

## 追加：/getglb 改为返回 JSON + base64（上一节的"响应格式不变"已作废）

用户反馈「响应体全是二进制，没找到实例名称」——因为节点名藏在 GLB 的 JSON chunk 里，
直接在 Postman/浏览器里看不见。确认后选择 **JSON + base64** 方案。

### 改动内容

- `app/schemas/catia.py` 新增 `GlbNode`（`id` / `instanceName` / `partNumber`）
  与 `GlbPayload`（`filename` / `parts` / `glb`）。
- `app/services/catia/service.py`
  - `get_glb()` 返回 `(bytes, filename, parts)`。
  - `_export_instances_to_glb` / `_export_reference_to_glb` 一并返回零件清单；
    新增 `_part_number(product)` 取引用零件号（`reference_product.part_number`，失败返回空串）。
  - `get_glb` 里把 `leaf_product` 提到 try 外面初始化，回退分支也能带上零件号。
- `app/api/v1/endpoints/catia.py`
  - `/getglb` 改为 `response_model=ApiResponse[GlbPayload]`，返回
    `{code,message,data:{filename, parts:[{id,instanceName,partNumber}], glb: base64}}`。
  - 移除 `quote` / `Content-Disposition` 下载头（用户已确认接受纯下载链接失效）。

### 验证

`_verify_getglb_json.py`：openapi 可生成（route `/api/v1/catia/getglb`，
schema `ApiResponse_GlbPayload_`）、用假数据调用端点得到正确的 JSON 结构、
`response_model` 校验通过、base64 能反解回原始字节。

### 遗留

- 实例名未去重（按用户选择"原样"）：同名实例在 GLB 里同名，前端按名匹配会一起高亮。
- 根目录 `_occ_name_test.py` / `_occ_name_test.glb` 是早期临时探测文件，可删。

---

## 追加：分支级导出问题调查（"选单个分支却返回整根线束"）

用户反馈：CATIA 里选单个分支（树里或 3D 视图），前端总是显示整根线束。

### 已定位的根因

`get_glb` 走的是 `ref_doc.ExportData(stp)` —— **整文档导出**。而一个「多分支」线束的
**所有分支几何都在同一个 CATPart 里**（见 MEMORY.md 的模型结构），所以选任何分支都导出整个
`MultiBranchable1.CATPart`。

### 探测结论（用真实 CATIA 会话，cable example.CATProduct）

- `SelectedElement.LeafProduct` 只给到最深的 **Product**（`多分支1.1`），不是最深几何体。
- 树里的 `分支.N` 是 ELW 特征，**未暴露给自动化**：`Value` / `Reference` 一律解析为 `多分支1.1`；
  `Search("Name=分支*,all")` 能找到 4 个，但同样无法区分。
- `Reference.DisplayName` 只能给出 product 级路径。
- `Selection.Copy` + 新建零件 + `PasteSpecial("CATPrtResultWithOutPartLink")` **不可用**
  （剪贴板空 → 导出空白 3126 字节 STEP）；副作用：会话里留下 `ccp_dummy10~12_Format_*` 占位文档。
- 零件疑似可视化模式：`Search("Name=肋*,all")` 返回 0。

### 新增的临时脚本（都在项目根，后续可删）

`_diag_selection.py` / `_diag_export.py` / `_diag_part_tree.py` / `_diag_branch.py`
`_test_body_export.py` / `_check_session.py` / `_sample_selection.py`（选择变化采样器）

### 待办

等用户在真实会话里点击「分支.2」与 3D 视图中的分支，用 `_sample_selection.py` 采样结果
判断是否还存在可用的区分途径（如 3D 拾取点 `GetCoordinates` 做空间匹配）。

---

## 追加：分支级导出已实现（几何判定 + 拾取点）

采样结果（用户真实点击）给出了明确答案，据此完成了改造。

### 决定性事实

1. **树里点 `分支.N` → 拿不到分支号。** 采样期间该次点击没有产生任何新状态；
   配合之前 `Search("Name=分支*,all")` 的 4 个命中全部是
   `Type=Product / Value.Name=多分支1.1 / PartNumber=多分支1 / Products=0 /
   Ref.DisplayName=cable example/.../几何束1.1/多分支1.1/`，
   且 `几何束1.1` 下只有 1 个子节点 —— 确认 `分支.N` 是 CATPart 内部电气特征，
   自动化接口**完全不可见**。这是 CATIA 硬限制，不是代码 bug。
2. **3D 视图点分支 → 能拿到分支号。** 选中项 `Value.Name` /
   `Reference.Name` 形如
   `Selection_RSur:(Face:(Brp:(EhiBundleSegmentRib.2;0:(Brp:(ElecCurve.2;1);
   Brp:(GSMCircle.2;...))));...)`，里面的 `2` 就是分支号。
3. **STEP 里的实体顺序 = 分支顺序。** 整体导出的 STEP 含 4 个
   `MANIFEST_SOLID_BREP`，名字是 `几何体.1..4`（`\X2\51E04F554F53\X0\` = 几何体）。
   用真实拾取点验证：点 Rib.2 → 到第 2 个实体距离 **0.0000**，到其它 ≥75；
   点 Rib.1 → 到第 1 个实体 **0.0000**，到其它 ≥51。**顺序与分支号严格一致**。
4. `read_step_file_with_names_colors` 拿到了名字但只有 `多分支1` 一层
   （XDE 不把 4 个实体当命名节点），所以名字走不通 → 改用**几何判定**。

### 改动内容

- `app/utils/step_to_gltf.py`
  - 新增 `StepEntry` dataclass（`step_path/matrix/name/branches/pick_points`），
    `_as_entry()` 兼容旧的 2/3 元组调用（不破现有调用方）。
  - 新增 `_iter_top_solids()` / `_distance_to_point()`（`BRepExtrema_DistShapeShape`）/
    `_merge_solids()` / `select_branch_geometry()`。
  - **裁剪优先级**：拾取点几何判定 > 分支序号；都不可用时保留整个零件（并 warning）。
  - 先应用实例矩阵、再判距离，这样总成坐标系的拾取点才能直接比较。
  - `step_to_gltf(..., branches=, pick_points=)`、`steps_to_gltf(..., branches=, pick_points=)`
    新增可选参数（函数级默认值 + entry 级覆盖）。**节点名仍是实例名，契约不变。**
- `app/services/catia/service.py`
  - `_BRANCH_NAME_PATTERN = re.compile(r"(?:EhiBundleSegmentRib|ElecCurve|GSMCircle)\.(\d+)")`
  - `_branch_index(item)`：从 `Value.Name` / `Reference.Name` 解析分支号。
  - `_pick_point(item)`：`SelectedElement(item).get_coordinates()`；`Type=="Product"` 或
    `(0,0,0)` 视为无效（非几何选择的兜底值）。
  - `_branch_selection(selection, count, leaf_name)`：只收集**与目标叶子零件同名**的选中项，
    多选时取并集 → 支持"选中 2~3 个断开的分支"。
  - `get_glb()` 返回 4 元组 `(bytes, filename, parts, branches)`；两个 `_export_*` 同步改签名，
    且**只把裁剪信息给被选中的那个实例**（多实例时避免误裁）。
- `app/schemas/catia.py`：`GlbPayload` 新增 `branches: list[int] = []`（空 = 整零件导出）。
- `app/api/v1/endpoints/catia.py`：解包 4 元组并透传 `branches`。

### 验证（离线，不依赖 CATIA）

- `_verify_branch_glb.py`（真实 STEP + 真实拾取点）：
  整根 21947 顶点 / 797KB；`branches=[2]` → 5823；`pick_points=[Rib.2]` → 5823（**完全一致**）；
  `[Rib.2, Rib.1]` → 10128 = 5823+4305（**精确相加**）；旧 2 元组调用仍 21947。
- `_verify_branch_service.py`：4 个文件编译通过；`_branch_index` 对真实引用名给出 2 / 1 / None；
  多选并集 `[2,1]` 且不混入别的零件；树选中返回 `[]`（回退整零件）；
  openapi 里 `GlbPayload` 含 `branches`；端点 base64 往返一致。

### 环境坑（重要）

- PowerShell 工具的 stdout 经常被吞，**统一把结果写文件再 Read** 才可靠。
- `fastapi.testclient` 依赖 **httpx，本环境没装** → 改用直接调用端点函数验证契约。
- Bash 工具在本机是坏的（`ls`/`head` 都 not found），别用。
- CATIA 会话中途被一个**模态「打开」文件对话框**卡住（`GetActiveObject` 报"操作无法使用"，
  但进程还在）。定位方式：pywin32 `EnumWindows` 找 `class=#32770` 的窗口；
  用 `PostMessage(hwnd, WM_CLOSE)` 取消即可。PowerShell 的 `Add-Type` 被安全策略禁掉，用 Python。
- 残留文件已清理，只留 `_verify_branch_glb.py` / `_verify_branch_service.py`
  和测试用 `_tmp_branch/baseline_full.stp`（真实线束 STEP，可复跑离线验证）。

### 待办

树里点 `分支.N` 无法识别分支号（CATIA 限制）。当前行为：拿不到就导出整个零件。
需要用户决定回退策略（见回复中的问题）。

**2026-09-23 10:56 用户已确认**：在 3D 视图点分支后，前端只展示该分支，功能可用。
说明「拾取点几何判定」这条主路径在真实会话里跑通了。

---

## 追加：多分支选中时，GLB 每根分支一个独立命名节点（parts 每根一项）

用户反馈：选中 2 根分支（`branches: [1,4]`）时，`parts` 只有一项（父零件 `多分支1.1`），
前端无法区分/单独高亮两根分支。

**用户确认的口径**（AskUserQuestion）：
- 命名：**`父实例名#分支号`**，即 `多分支1.1#1`、`多分支1.1#4`
- **不保留**「整根线束」父节点，GLB 里只有分支节点

### 改动内容

- `app/utils/step_to_gltf.py`
  - 新增 `BRANCH_SEP = "#"`、`branch_node_name(instance_name, branch)`、
    `ExportedNode{name, instance_name, branch}` dataclass。
  - 新增 `split_branch_geometry()`：返回 `[(分支实体, 分支序号), ...]`（升序）；
    没命中/无裁剪要求时返回 `[]`，调用方据此保留整个零件。
    `select_branch_geometry()` 改为它的包装（合并版，保留兼容）。
  - 新增 `_add_entry_nodes()`：有分支信息且命中 → **每根分支各 `AddShape` 成一个独立命名节点**；
    否则整体一个节点（名 = 实例名）。
  - `step_to_gltf()` / `steps_to_gltf()` 改为**返回 `list[ExportedNode]`**（按分支拆分时每根一项），
    entry 内部走 `_add_entry_nodes`，不再各自调 `select_branch_geometry`。
- `app/services/catia/service.py`
  - 新增模块级 `_branches_of(nodes)`：取**实际**导出的分支号。
  - 新增 `CatiaService._nodes_to_parts(nodes, products)`：节点清单 → `parts`（每根分支一项），
    `id`/`instanceName` = `node.name`，外加 `branch`；`partNumber` 按 `node.instance_name` 查产品。
  - `_export_reference_to_glb` / `_export_instances_to_glb` 改为先取 `nodes` 再转 `parts`；
    后者用 `products: dict[instance_name -> inst]` 保存各实例以取零件号。
  - `get_glb()` 第 4 个返回值语义从「请求的分支号」改为「**实际**导出的分支号」。
- `app/schemas/catia.py`：`GlbNode` 新增 `branch: int | None = None`。
- `app/api/v1/endpoints/catia.py`：只更新注释（`GlbNode(**part)` 自动带上 `branch`）。

### 验证（离线，不依赖 CATIA）

`_verify_branch_nodes.py`（8 组，全部通过；用真实 `_tmp_branch/baseline_full.stp`）：

```
branches=[1,4]  -> 节点 ['多分支1.1#1','多分支1.1#4']，2 节点 / 10715 顶点
pick=[Rib.1,Rib.2] -> ['多分支1.1#1','多分支1.1#2']，2 节点 / 10128 顶点
不裁剪           -> ['多分支1.1']，1 节点 / 21947 顶点
旧二元组调用      -> ['part_0','part_1']（兼容不变）
越界 branches=[9] -> []（回退整零件）
parts 条数 == branches 条数: True
openapi GlbNode 字段: ['id','instanceName','branch','partNumber']
```

顶点数交叉验证：`#1=4305`、`#2=5823`、`#4=6410` → `4305+5823=10128`、`4305+6410=10715`，
与之前单分支实测完全一致，说明分割**无漏无重**。

### 影响面

`parts[].id` 对分支场景由 `多分支1.1` 变为 `多分支1.1#N`；树选中（拿不到分支号）时**仍是**
`多分支1.1` 不变。前端按 `parts[].id` 匹配 `node.name` 的逻辑无需改写，但若它原本拿
`/getselected` 的 `name` 去比对，需要处理 `#N` 后缀。

---

## 追加：多选（分支 + 卡扣）一次全部返回 + 修掉误裁普通零件的坑

用户反馈：选「一根分支 + 一个卡扣」时只显示一根分支。

### 先用真实会话查清事实

`_diag_sel_now.py` 读到用户当时的选中：

```
item 1: Face  EhiBundleSegmentRib.3 ...  LeafProduct=多分支1.1  pick=(-93.946, 313.594, 101.738)
item 2: PlanarFace  Shaft.1 ...          LeafProduct=clipA.2   pick=(-36.293, 233.326, 52.824)
```

→ 两个不同 CATPart。**根因**：`get_glb(index)` 只取 `selection.Item2(index)`，默认 index=1，
所以第 2 项（卡扣）整条被丢掉。

`_diag_xform.py` 顺便量了变换：`clipA.2` / `多分支1.1` 的**祖先位移全为 0**，
所以当前"只累加到选中节点自身"的矩阵在本案例无误差，跨零件合并不会错位（见 MEMORY.md 限制条）。

### 改动内容

- `app/services/catia/service.py`
  - `get_glb(index: int = 0)`：**index <= 0 导出全部选中项**（可跨零件）；index >= 1 只导第 index 项。
  - 选中项按 `LeafProduct.Name` **分组**（保持顺序），每组调用新的 `_items_to_entries()`，
    各组条目汇总后**一次** `steps_to_gltf` 写出 → 一个 GLB 含所有选中项。
  - 新 `_items_to_entries(tmp_dir, items, seq)` 取代 `_export_reference_to_glb` /
    `_export_instances_to_glb`（两者已删除，它们各自写 GLB，无法合并）。
  - `_branch_selection(items)` 签名改为直接收条目列表（不再自己遍历 selection）。
  - **新增裁剪切入口**：`if pick_points and not branches: pick_points = []` —— 普通零件上的一次
    面点击不再被当成分支信号。
- `app/api/v1/endpoints/catia.py`：`index: int = Body(0)`（原 `Body(1)`）+ 注释。

### 为什么必须要那个切入口

否则选中卡扣时会走 `split_branch_geometry(shape, [], [点击点])` → 按"最近实体"裁剪，
节点名变成 `clipA.2#1`、`parts` 里多一个假的 `branch: 1`（卡扣只有 1 个实体，几何上看不出来，
名字已经错了）。**只有 `_BRANCH_NAME_PATTERN` 命中时才允许裁剪。**

### 验证（离线，桩对象驱动真实 `get_glb`）

`_tmp_branch/` 下用 `_grab_steps.py` 预导出 `harness_full.stp`（整根线束）与 `clipA_full.stp`（卡扣），
`_verify_multi_selection.py` 用假 selection/app 驱动真实 `CatiaService.get_glb()`：

```
get_glb()        -> parts=[多分支1.1#3(branch=3), clipA.2(branch=None)]  branches=[3]  204200 字节
                    GLB 节点 [('多分支1.1#3', 5274), ('clipA.2', 294)]，与 parts[].id 完全一致
get_glb(index=1) -> 只有 多分支1.1#3      （旧行为保留）
get_glb(index=2) -> 只有 clipA.2
index=3          -> CatiaError 选中项索引 3 需在 0..2 之间（0 = 全部）
空选择            -> CatiaError 请先在 CATIA 中选中至少一个零件/分支
监听导出层入参：线束条目 branches=[3] pick_points=[(-93.9,313.6,101.7)]；
                卡扣条目 branches=[] pick_points=[]  ← 切入口生效
```

`_verify_branch_nodes.py` 重跑无回归（8 组仍全过）。
`clipA` 顶层实体数 = 1、`harness` = 4。

### 遗留

- 服务层裁剪只在识别到分支特征时生效：若某天 CATIA 的面引用名里不再带
  `EhiBundleSegmentRib.N`，就会退化成"导出整根线束"。届时可考虑放宽判定条件。
- 变换链不含祖先位姿（见 MEMORY.md）。



